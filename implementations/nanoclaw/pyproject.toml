[project]
name = "exaone-impl-nanoclaw"
version = "0.0.0"
description = "EXAONE Proof Gallery — NanoClaw glue scripts (isolated uv env)"
requires-python = ">=3.10"
dependencies = [
    "exaone",
    "requests>=2.28.0",
]

[tool.uv]
package = false

[tool.uv.sources]
exaone = { path = "../../exaone", editable = true }
