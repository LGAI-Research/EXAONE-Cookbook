[project]
name = "exaone-impl-smolagents"
version = "0.0.0"
description = "EXAONE Proof Gallery — smolagents glue (isolated uv env)"
requires-python = ">=3.10"
dependencies = [
    "exaone",
    "smolagents[openai]==1.26.0",
]

[tool.uv]
package = false

[tool.uv.sources]
exaone = { path = "../../exaone", editable = true }
