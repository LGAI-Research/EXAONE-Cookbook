# submodules/ — upstream clones (user-managed)

이 디렉터리는 **cookbook 레포에 포함되지 않습니다.**  
Proof Gallery(`implementations/`)를 쓰려면 아래 upstream을 **직접 clone** 하세요.

가이드 정본: [`implementations/README.md`](../implementations/README.md) § Upstream clone.
